apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  generateName: scan-docker-
spec:
  serviceAccountName: dockerhub-service
  taskRef:
    name: twistlock-scan 
  params:
    - name: pathToDockerFile
      value: Dockerfile
  resources:
    inputs:
      - name: docker
        resourceRef:
          name: hello-tekton-git
    outputs:
      - name: Twistlock-Image
        resourceRef:
          name: twistlock-image